name: tenant-isolation-nightly
on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: tenant-isolation-nightly
  cancel-in-progress: false
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: finnor-os
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: finnor-os/package-lock.json
      - run: npm ci
      - name: Probe canonical production PE tenant isolation
        env:
          TENANT_A_PROBE_JWT: ${{ secrets.TENANT_PROBE_A_JWT }}
          TENANT_B_PROBE_JWT: ${{ secrets.TENANT_PROBE_B_JWT }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.TENANT_PROBE_VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          export PRODUCTION_API_URL="$(node -p "require('../infra/deployment/production.contract.json').topology.api.productionUrl")"
          npx tsx scripts/probe-tenant-isolation.ts
