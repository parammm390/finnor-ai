/** Production handler registry is the shared durable compute contract. Keep this
 * compatibility module so existing tests/importers resolve one source of truth. */
export { PRODUCTION_JOB_CONTRACTS } from "@finnor/db";
export type { ProductionJobType } from "@finnor/db";
